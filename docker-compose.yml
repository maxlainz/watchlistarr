services:
  watchlistarr:
    image: maxlainz/watchlistarr:latest
    container_name: watchlistarr
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-8080}:8080"
    volumes:
      - ./data:/data
    env_file:
      - .env
